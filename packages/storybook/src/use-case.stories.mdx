import { Meta, Canvas, Story } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import { Reshoot } from '@reshoot/react';
import describe from './utils/describe';
import Gallery from './utils/Gallery';

import '@reshoot/react/lib/styles.css';
import './utils/docs.css';

<Meta
  title="Live Demo/Use Case"
  parameters={{
    layout: 'padded',
    previewTabs: { 'storybook/docs/panel': { disable: true, hidden: true } },
  }}
/>

export const Template = ({ state, ...args }) => (
  <Gallery _s={state} {...args} />
);

export const argTypes = {
  state: {
    name: 'State',
    control: {
      type: 'select',
      options: {
        AUTO: null,
        MANUAL: 0,
        OFFLINE: 1,
        ERROR: 2,
        LOADING: 3,
        FADING: 4,
        LOADED: 5,
      },
    },
  },
  disablePlaceholder: { table: { disable: true } },
};

<Canvas>
  <Story
    name="Gallery (LQIP)"
    args={{ state: null, disablePlaceholder: false }}
    argTypes={argTypes}
    inline
  >
    {Template.bind()}
  </Story>
  <Story
    name="Gallery (Color)"
    args={{ state: null, disablePlaceholder: true }}
    argTypes={argTypes}
    inline
  >
    {Template.bind()}
  </Story>
</Canvas>
